---
'DJMTbot': minor
---

- adds `GuildChannelManager` and `GuildConfigManager`
- replaces all usage of deprecated `ephemeral` usage in replies to use `flag` instead
- adds a `change` field to jsonSave logging to see what changed in a saveJSON call.
