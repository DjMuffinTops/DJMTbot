Changesets usage

- Create a changeset: `pnpm changeset` and follow the prompts.
- Create a version (generate changelog & version bumps): `pnpm run version`.
- Publish (requires auth): `pnpm run publish`.

See https://github.com/changesets/changesets for details.
